export default class Recommend {
    constructor({id, name, desc, avatar, num}) {
        this.id = id
        this.name = name
        this.desc = desc
        this.avatar = avatar
        this.num = num
    }
}